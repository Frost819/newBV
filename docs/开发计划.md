# new BV 开发计划

> 配套文档：[PRD](PRD.md) | [AGENTS.md](../AGENTS.md)
> 本文档将 PRD 的需求分解为可执行的开发任务，按阶段推进，每阶段含明确验收标准。

---

## 总体节奏

new BV 采用 **4 个阶段** 迭代开发，与 PRD 第 9 章的 Milestone 对齐：

| 阶段 | 名称 | 目标 | 对应 PRD Milestone |
|---|---|---|---|
| Phase 0 | 基础架构搭建 | 项目骨架可编译可运行 | M1 |
| Phase 1 | 核心功能（VOD） | 完整点播观看流程，对标原版 BV | M2 |
| Phase 2 | 新增核心功能 | 直播 + 触屏 + 评论 + 主题 + 日志体系 | M3 |
| Phase 3 | 增强与打磨 | P2 功能 + 稳定性 + 性能优化 | M4 |

**测试策略**：每个任务必须同步产出测试，不允许"先功能后补测"。测试不通过的 PR 不合并。

---

## Phase 0：基础架构搭建（M1）

**目标**：搭建项目骨架，所有模块可编译，应用可启动，Navigation 可跳转，主题可切换。

**预计工作量**：基建任务，无功能交付，但为后续所有开发奠基。

### P0-1 项目配置与模块搭建 ✅

**任务**：
1. 配置根 `build.gradle.kts` ✅：Kotlin 2.4.10、KSP 2.3.10、AGP 9.1.1、Hilt 2.60.1+、Java 17
2. 配置 `settings.gradle.kts` ✅：添加 8 个模块（`:app, :core, :data, :player, :danmaku, :bili-api, :bili-api-grpc, :bili-subtitle`）
3. 配置 `gradle/libs.versions.toml` ✅：统一版本目录（所有插件+依赖），移除 `gradle.versions.toml` 独立目录
4. 配置 `app/build.gradle.kts` ✅：
    - applicationId `dev.frost819.newbv`
    - compileSdk 36 / minSdk 21 / targetSdk 36
    - Compose、Hilt、Navigation-Compose、Coil 3.x、Room、DataStore、Media3 依赖
    - 测试依赖（JUnit 5 + MockK + Turbine + Truth + kotlinx-coroutines-test）
    - `useJUnitPlatform()` 配置
    - ktlint + detekt 插件（已引用，规则待配置）
5. 配置 JaCoCo 覆盖率插件（待 P0 全部完成后统一配置）
6. 配置 `local.properties.template` ✅
7. 创建所有子模块 `build.gradle.kts` + 目录结构 ✅
8. 包名 `dev.frost819.newbv`（原计划 `dev.newbv`，已改为带前缀）
9. **AGP 9.x 适配**：移除所有 Android 模块的 `org.jetbrains.kotlin.android` 插件（AGP 9.0+ 内置）
10. **Gradle 9.x 适配**：移除 `settings.gradle.kts` 中重复的 `versionCatalogs` 声明（Gradle 9.x 自动发现 `gradle/libs.versions.toml`）
11. 复制 `bv/bili-api-grpc/proto/` 122 个 proto 文件到 `:bili-api-grpc/proto/`
12. 创建 `buildSrc`：`AppConfiguration.kt`（appId、SDK 版本）、`ProtobufConfiguration.kt`（proto 排除列表）
13. 创建 app 最小源码：`BVApplication`、`MainActivity`（`BVTheme` + `Greeting`）
14. gradle.properties：关闭 `configuration-cache`（git exec 不兼容）、开启 `parallel` + `caching`

**未完成项**：
- JaCoCo 配置（覆盖率目标 ≥80%，P0 全部完成后统一配置）
- ktlint 规则文件（`.editorconfig` 或 `config/`）
- detekt 规则文件

**验收**：
- ✅ `./gradlew assembleDebug` 成功（42MB debug APK）
- ✅ `./gradlew :core:test` 通过（30 个单元测试）
- ✅ 所有 8 个模块编译通过
- ✅ 包名 `dev.frost819.newbv` 验证正确（APK 中 class 路径确认）

**测试**：无（配置任务）

### P0-2 core 模块：主题与交互模式 ✅

**任务**：
1. 创建 `:core` 模块 ✅
2. 实现主题系统： ✅
   - `BVTheme.kt`：双层 MaterialTheme（TV + Common），支持 dark/light colorScheme ✅
   - `Color.kt`：B 站粉 `#FB7299` 品牌色板 + 深色/浅色中性色 ✅
   - `Typography.kt`：TV Material3 + 普通 Material3 双 Typography ✅
   - `ThemeMode` 枚举（FollowSystem / Dark / Light）+ `isDark()` ✅
3. 实现交互模式抽象： ✅
   - ~~`InteractionMode` 枚举（DPAD / TOUCH）~~ → 已重构为 `InputMethod` + `InteractionTracker`
   - ~~`rememberInteractionMode()`~~ → 已重构为 `currentInputMethod()` + `LocalInteractionTracker`
   - **设计变更**：不根据设备能力互斥选择，而是始终同时支持两种输入，运行时追踪最近输入方式
   - `InteractionTracker`：`StateFlow<InputMethod>`，由 Activity 事件驱动（`onTouch()` / `onDpadKey()`）
   - `focusedBorder` 根据 `InputMethod` 动态显示/隐藏（触屏→隐藏，遥控器→显示）
4. 实现焦点管理工具： ✅
   - `focusedBorder()`：获焦边框（支持呼吸动画，根据输入方式动态显示）
   - `focusedScale()`：获焦放大效果
   - `requestFocus(scope)`：失败重试的焦点请求
   - `KeyEventExt`：D-Pad 方向键 / 确认 / 返回判断扩展
5. 实现日志基础设施： ✅
   - `core/log/Logger.kt`：自建 Logger 抽象和 Android Logcat 实现
   - `bili-api/.../util/BiliLogger.kt`：JVM 模块 `println` 日志实现
   - `LogFormat`：崩溃日志和手动日志头部格式化
   - `CrashHandler`：`UncaughtExceptionHandler` + 设备信息 + Logcat

**验收**：
- ✅ 主题可在 dark/light 间切换
- ✅ `InputMethod` 运行时追踪，同时支持触屏+遥控器
- ✅ 单元测试覆盖 `InteractionTracker`、`LogFormat`、`ThemeMode`、`Logger`、`BiliLogger` 逻辑

**测试**（43 个单元测试 + 10 个插桩测试全部通过）：
- `ThemeModeTest`（7）：isDark / fromOrdinal / displayName
- `InteractionTrackerTest`（8）：状态流转 / StateFlow 传播 / 去重 / 属性
- `LogFormatTest`：日志头部 / 崩溃头部格式
- `LoggerTest`（9）：各日志级别 / 懒加载 / 异常安全
- `BiliLoggerTest`（4）：前缀验证 / 异常堆栈 / 全方法覆盖
- `CrashHandlerTest`（插桩）：崩溃日志创建 / 手动日志 / 清理逻辑 / handler 安装卸载 / 设备信息写入 / Logcat 输出

### P0-3 data 模块：Room + DataStore ✅

**任务**：
1. 创建 `:data` 模块
2. 实现 Room 数据库：
   - `AppDatabase`（version 4）
   - `UserDB` / `UserDao`（多账户）
   - `SearchHistoryDB` / `SearchHistoryDao`
   - `Converters`（Date ↔ Long）
3. 实现 DataStore Prefs：
   - `Prefs` 对象 + `PrefDelegate`（参考原版，迁移到 Hilt 注入）
   - 全部设置项（见 PRD 第 7 章）
   - buvid / buvid3 自动生成
4. Repository 接口定义（实现在 app 层）

**验收**：
- ✅ Room 数据库可建表、迁移
- ✅ DataStore 可读写偏好
- ✅ DAO 单元测试 + 插桩测试通过

**测试**：
- ✅ `UserDaoTest`（插桩）：CRUD — 编译通过，需设备运行
- ✅ `SearchHistoryDaoTest`（插桩）：插入、查询、删除、按时间倒序 — 编译通过，需设备运行
- ✅ `PrefsTest`（单元）：默认值、读写、buvid 生成 — 63 个测试通过
- ✅ `BuvidGeneratorTest`（单元）：buvid 格式、MD5 算法 — 10 个测试通过

### P0-4 bili-api 模块迁移（去代理、去 Firebase）✅

**任务**：
1. 复制 `bv/bili-api/` 到 `:bili-api`
 2. 包名改为 `dev.frost819.newbv.biliapi`
3. **删除代理相关代码**：
   - `BiliHttpProxyApi.kt`
   - `ChannelRepository.proxyChannel` / `initProxyChannel`
   - `ProxyArea` 相关实体与逻辑
   - `ProxyWebPlayUrlData` / `ProxyAppPlayUrlData`
4. **删除 Firebase 相关**
5. 将 Koin 注入改为 Hilt（`@Module @InstallIn(SingletonComponent)`）
6. 保留 WBI 签名、App 签名、buvid 生成、UA 设置逻辑
7. 保留所有 HTTP API、gRPC Channel、Repository、Entity
8. 复制 `bv/bili-api-grpc/` 到 `:bili-api-grpc`（proto 文件）
 9. 复制 `bv/bili-subtitle/` 到 `:bili-subtitle`，包名改 `dev.frost819.newbv.bilisubtitle`
10. **删除 BiliPlus 模块**（`BiliPlusHttpApi.kt`、`entity/biliplus/View.kt`）— 第三方 API，不再支持

**验收**：
- ✅ 模块可编译
- ✅ 原 `bili-api` 测试可运行（凭证从 `local.properties` 读取）
- ✅ 代理代码完全移除，无编译残留

**测试**：
- ✅ 迁移原 `bili-api` 所有测试（HTTP API 测试、Repository 测试、Entity 测试）
- ✅ `bili-subtitle` 测试（已有 `SubtitleParserTest` / `SubtitleEncoderTest`，迁移即可）— 6 个测试全部通过
- ✅ 修复测试初始化：移除 Koin 后需显式调用 `BiliHttpApi.init(BUVID)`（10 个 Repository 测试）
- ✅ 修复 DanmakuMaskTest 硬编码路径 `File("")` → `System.getProperty("java.io.tmpdir")`
- ✅ 修复 4 个上游接口格式变更（SearchResultItem.play、SearchResultData、AddCoin.like、RegionLocs.data）
- ⬜ **App 接口（gRPC）测试**：因缺少 `access_token`（App 端登录凭证已过期），以下测试暂未通过：
  - FavoriteRepositoryTest × 6（`with token` 系列）
  - SeasonRepositoryTest × 1（`app api`）
  - UserRepositoryTest × 2（`app api` / `grpc api`）
  - BiliHttpApiTest × 2（需 accessKey + 测试视频已删除）
  - **后续计划**：通过模拟器运行 App 登录流程获取新 `access_token`，再补测 App 接口
- ⬜ **补全**：原版测试是集成测试，新增 Mock 版单元测试（Mock BiliHttpApi，验证业务逻辑）— 待后续补全
- ✅ `ApiSignTest` + `BuvidTest`（单元）：WBI 签名算法、buvid 生成
- ✅ `PlayDataTest`（单元，53 tests）：fromPlayUrlData 解析/plus 合并去重排序/DashVideo-DashAudio 字段映射
- ✅ `LikeRepositoryTest`（单元，11 tests）/ `CoinRepositoryTest`（单元，11 tests）/ `OneClickTripleActionRepositoryTest`（单元，7 tests）— Mock BiliHttpApi 单例
- ⬜ `DanmakuMask` 解析单测 — 待后续补全

### P0-5 player 模块迁移 ✅

**任务**：
1. ✅ 复制 `bv/bv-player/` 到 `:player`
  2. ✅ 包名改 `dev.frost819.newbv.player`
3. ✅ 播放器抽象：
   - `AbstractVideoPlayer` 抽象类（保留原版接口设计）
   - `ExoMediaPlayer` 实现（Media3 / ExoPlayer）
   - VOD：`MergingMediaSource`（视频+音频合并）
   - Live：`ProgressiveMediaSource`（HLS/FLV，已在 `playUrl` 中支持）
4. ✅ 保留 `OkHttpUtil`（自定义 SSL + bilivideo 域名验证）
5. ✅ 保留 `VideoPlayerOptions`、`VideoPlayerListener`、`BvVideoPlayer` Composable
6. ✅ 删除 VLC 相关代码与依赖（原版 build.gradle 已无 VLC，本版从未引入）
7. ✅ 编解码枚举迁移：`VideoCodec`、`VideoResolution`（原 `Resolution`），移除 Android 资源依赖改为纯字符串
8. ✅ 删除 BiliPlus 模块（P0-4 已完成）
9. ✅ 无代理 / Ali CDN 代码（原版 bv-player 无此逻辑，代理在 app 层 ViewModel 中）

**验收**：
- ✅ 模块可编译
- ✅ `assembleDebug` 全量构建通过
- ⬜ `ExoMediaPlayer` 可播放 DASH 视频（需插桩测试，待模拟器验证）
- ⬜ `ExoMediaPlayer` 可播放 HLS/FLV 直播流（需插桩测试，待模拟器验证）

**测试**：
- ✅ `VideoCodecTest`（单元）：编码枚举、codecId 映射、fromCodecString、toBiliApiCodeType — 9 tests pass
- ✅ `VideoResolutionTest`（单元）：画质枚举、fromCode、roundtrip — 8 tests pass
- ✅ `ExtendsTest`（单元）：formatMinSec 时间格式化 — 2 tests pass
- ✅ `ExoMediaPlayerTest`（插桩）：播放器生命周期、isPlaying、seekTo、speed、duration/currentPosition、bufferedPercentage、videoDimensions、tcpSpeed、debugInfo、playUrl、stop/release、setOptions、softwareDecoder — 15 tests pass（100%）
- ✅ `OkHttpUtilTest`（插桩）：SSL 配置、bilivideo.com/bilivideo.cn 域名验证 — 5 tests pass（100%）
- ⬜ `ExoMediaPlayer` 真实流播放（插桩）：DASH 视频、HLS/FLV 直播流播放 — 待后续（需网络 + 真实测试视频）
- ⬜ 编解码选择逻辑单测（Mock PlayData，验证选择策略）— 待后续，需 app 层 ViewModel 重构后

### P0-6 danmaku 模块抽取 ✅

**任务**：
1. ✅ 创建 `:danmaku` 模块
2. ✅ 从 `bv/app` 抽取弹幕相关代码：
   - `DanmakuPlayerCompose`（Compose 封装，`component/DanmakuPlayerCompose.kt`）
   - `DanmakuMaskFinder` + `calculateMaskDelay`（蒙版处理，`util/DanmakuMaskFinder.kt`）
   - `MaskModifiers`（蒙版 Modifier：`bitmapMask` / `danmakuWebMask` / `danmakuMobMask` / `danmakuMask`，`util/MaskModifiers.kt`）
3. ✅ 弹幕数据模型迁移：
   - `DanmakuType`（`entity/DanmakuType.kt`）— 移除 Android 资源依赖，改为纯 modeValue 映射
   - `DanmakuSpeedFactor`（`entity/DanmakuSpeedFactor.kt`）— 移除 Android 资源依赖，改为纯 factor 值
4. ✅ 弹幕配置抽象（`config/DanmakuState.kt`）— 包含 scale/opacity/area/speedFactor/maskEnabled/enabledTypes

**验收**：
- ✅ 模块可编译
- ✅ `assembleDebug` 全量构建通过
- ✅ 弹幕可渲染（插桩测试验证）— DanmakuPlayerCompose + MaskModifiers 14 tests pass（100%）

**测试**：
- ✅ `DanmakuTypeTest`（单元）：fromAkDanmakuMode 转换、modeValue 常量 — 10 tests pass
- ✅ `DanmakuSpeedFactorTest`（单元）：factor 值、fromFactor 查找、默认值 — 4 tests pass
- ✅ `DanmakuStateTest`（单元）：默认值、isShowAll 逻辑、screenPart/alpha/textSizeScale 映射、copy — 8 tests pass
- ✅ `DanmakuMaskFinderTest`（单元）：web/mob mask 帧查找、segment 缓存、reset — 6 tests pass
- ✅ `CalculateMaskDelayTest`（单元）：有/无蒙版 + 播放/暂停 延迟计算、clamp 范围 — 6 tests pass
- ✅ `DanmakuPlayerComposeTest`（插桩）：null/player 渲染、player 生命周期 — 3 tests pass
- ✅ `MaskModifiersTest`（插桩）：bitmapMask 渲染、mobMask 像素解码（全透明/全黑/混合）、webMask SVG 解析、danmakuMask 统一入口（null/mob/web）— 11 tests pass

### P0-7 app 骨架：单 Activity + Navigation + Hilt ✅

**任务**：
1. ✅ `BVApplication`（`@HiltAndroidApp`）：
   - 初始化 Hilt
   - `Prefs.init()`
   - 初始化 `CrashHandler`（通过 Hilt 注入）
   - 初始化 `BiliHttpApi`（通过 NetworkModule，buvid3 生成）
   - 注意：Ktor 本地日志服务器推迟到 P0-8
2. ✅ `MainActivity`（`@AndroidEntryPoint`）：
   - SplashScreen
   - 交互模式追踪（`InteractionTracker` 注入，触屏/遥控器自动切换）
   - `NavHost` 设置（`AppNavHost`）
3. ✅ Navigation 路由定义（`@Serializable` Route data class）：
   - `HomeRoute`、`SearchRoute`、`VideoDetailRoute`、`VideoPlayerRoute`、`SeasonPlayerRoute`、`LivePlayerRoute`、`UserSpaceRoute`、`PgcFeatureRoute`、`SettingsRoute`、`UserSwitchRoute`、`LoginRoute`
4. ✅ 空白 Screen 占位（每个路由一个 `PlaceholderScreen`）
5. ✅ Hilt Module：
   - `NetworkModule`（BiliHttpApi、AuthRepository、InteractionTracker、CrashHandler）

**验收**：
- ✅ 应用可启动，显示 SplashScreen → HomeScreen
- ✅ Navigation 可在占位页间跳转（路由注册完整）
- ✅ Hilt 注入正常（插桩测试验证无运行时异常）
- 注意：用户锁功能推迟到后续阶段实现

**测试**：
- ✅ `RoutesTest`（单元）：11 条路由的序列化/反序列化、默认值验证 — 16 tests pass
- ✅ `BVApplicationTest`（插桩）：Application 上下文可用、Hilt 注入正常 — 2 tests pass
- ✅ `MainActivityTest`（插桩）：Activity 可启动、不崩溃 — 2 tests pass
- 注：删除了遗留的 `ExampleInstrumentedTest`（包名不匹配）

### P0-8 Ktor 本地日志服务器 ✅

**任务**：
1. ✅ 迁移原版 `HttpServer.kt`（Ktor CIO）— 改为 Hilt 注入的 class（非 object 单例）
2. ✅ 路由：
   - `GET /` — 日志管理 UI（index.html）
   - `GET /logs_ui/{path...}` — 静态资源（CSS/JS/字体，含路径穿越防护）
    - `GET /api/logs/list` — 日志文件列表 JSON（含 `logs_manual_*` / `logs_crash_*`）
   - `GET /api/logs/{filename}` — 下载（白名单：前缀+后缀校验，路径穿越防护）
   - `GET /api/logs/create-manual-and-download` — 创建手动日志并下载
3. ✅ 静态资源：`assets/logs_ui/`（index.html + server.js + mdui CSS/JS/字体）
    - 网页端保留手动日志和崩溃日志两个标签页
4. ✅ 端口：随机端口（port = 0），通过 `getPort()` 获取实际端口
   - 注意：通知/设置页展示访问地址推迟到后续 UI 阶段
5. ✅ Hilt 集成：`NetworkModule` 提供 `HttpServer` 单例，`BVApplication` 注入

**验收**：
- ✅ 日志服务器可启动（`start()` / `stop()` / `isRunning()` / `getPort()`）
- ✅ 浏览器可访问日志列表与下载（API 路由验证通过）

**测试**：
- ✅ `HttpServerTest`（单元）：18 tests pass
  - 服务器启动/停止/端口解析
  - 首页路由（200/404）
  - 静态资源路由（200/403 路径穿越/404）
  - `/api/logs/list`（列表 JSON、排序、空列表）
   - `/api/logs/{filename}`（下载、404 不存在、403 白名单/后缀/路径穿越）
  - `/api/logs/create-manual-and-download`（创建下载、500 失败）

### Phase 0 验收标准

- [x] `./gradlew assembleDebug` 成功 — P0-1 ✅（42MB debug APK）
- [x] `./gradlew test` 全部通过（core 43, data 73, bili-subtitle 6, player 19, danmaku 34, app 34 单元测试通过；bili-api 集成测试已分离为 `integrationTest` 任务）
- [x] `./gradlew ktlintCheck detekt` 通过 — ✅（`.editorconfig` + `config/detekt.yml` 配置完成，迁移代码复杂度规则已合理禁用）
- [x] 应用可启动，Navigation 可跳转占位页（P0-7）✅
- [x] 主题可切换（dark/light）— P0-2 ✅
- [x] Hilt 注入正常（P0-7）✅ — 插桩测试 4 tests pass
- [x] Room + DataStore 可读写（P0-3）✅ — 插桩测试 20 tests pass（SearchHistoryDao 10 + UserDao 10）
- [x] bili-api 模块迁移完成，代理代码完全移除（P0-4）
- [x] player 模块可播放 DASH 视频（P0-5）✅ — 流播放插桩测试 6 tests pass（asset 视频：prepare/play/seek/pause/stop/restart + videoFormat 验证），真实流播放待后续
- [x] danmaku 模块可编译 + 单元测试通过（P0-6）— 34 tests pass（100%），插桩测试待后续
- [x] 日志服务器可启动（P0-8）✅ — 18 tests pass
- [x] 测试覆盖率报告可生成（JaCoCo）✅ — `./gradlew jacocoAggregatedReport` 生成聚合报告
- [x] CrashHandler 插桩测试通过 ✅ — 10 tests pass（崩溃日志创建、手动日志、清理逻辑、handler 安装/卸载）

---

## Phase 1：核心功能（VOD，M2）

**目标**：完整点播观看流程，功能对标原版 BV。用户可登录、浏览、搜索、播放视频（含弹幕字幕）、管理个人页。

### P1-1 登录与账户 ✅

**任务**：
1. `LoginScreen` + `QrLoginContent`：
   - TV QR 登录（`passport-tv-login/qrcode/*`）
   - Web QR 登录（`passport-login/web/qrcode/*`）
   - 轮询登录状态
   - 二维码显示
2. `UserSwitchScreen`：多账户管理
   - 账户列表、添加、删除、切换
   - 管理模式（显示 Token、锁设置、删除）
3. `UserPanel`：用户信息弹窗
   - 用户名、等级、EXP 进度
   - 无痕模式开关
   - 关注列表入口
   - 账户管理入口
4. 用户锁：
   - `UnlockUserScreen`：启动解锁（D-pad 方向密码）
   - `UserLockSettingsScreen`：设置/修改密码
5. `AccountRepositoryImpl`：登录态管理（Hilt 注入，聚合 UserDao + Prefs + AuthRepository）
6. `AuthRepository`：内存鉴权状态（已有，迁移自原版）

**验收**：
- [x] QR 登录可用（TV QR + Web QR）
- [x] 多账户切换可用
- [x] 用户锁可用
- [x] 无痕模式可切换

**测试**：
- [x] `LoginViewModelTest`（单元，8 tests）：QR 状态机流转、轮询、取消、重置
- [x] `AccountRepositoryImplTest`（单元，16 tests）：CRUD、登录/登出、切换、锁、Prefs 同步
- [x] `AuthDataTest`（单元，6 tests）：JSON roundtrip、Prefs 双向同步
- [x] `UserSwitchViewModelTest`（单元，4 tests）：列表加载、切换、删除
- [ ] `QrLoginContentTest`（插桩）：UI 状态流转、二维码显示 — 延后到集成测试
- [ ] `UserSwitchScreenTest`（插桩）：账户列表、切换、删除 — 延后到集成测试
- [ ] `UnlockUserScreenTest`（插桩）：密码输入、解锁 — 延后到集成测试

**实现备注**：
- `QRCode.render()` 返回 `QRCodeGraphics`，通过 `nativeImage() as android.graphics.Bitmap` 获取位图
- `Prefs.resetForTesting()` 改为 `@VisibleForTesting` public，使 app 模块测试可访问
- `LoginViewModel` 使用 `viewModelScope.launch` + `delay(1000)` 轮询，`UnconfinedTestDispatcher` 控制虚拟时间
- `AccountRepositoryImpl` 在 `init` 中从 Prefs 恢复登录态，同步到 `AuthRepository`
- `addUser` 调用 `refreshUserInfo()` 从 `BiliHttpApi.getUserSelfInfo` 获取用户名/头像/等级

### P1-2 主页导航与首页 ✅

**任务**：
1. `MainScreen`：左侧 NavigationDrawer（6 项含直播入口）
   - 左侧栏：头像 + 6 导航项 + 设置
   - 内容区切换（AnimatedContent）
   - 退出确认（双击返回）
2. `HomeScreen`：
   - 顶部 Tab（动态/推荐/热门，可排序）
   - `RecommendScreen`：4 列网格 + 无限滚动
   - `PopularScreen`：4 列网格 + 无限滚动
   - `DynamicsScreen`：动态视频（需登录）
   - 菜单键刷新
3. `HomeViewModel`：推荐/热门/动态数据加载
4. `SmallVideoCard`：视频卡片（封面+统计+标题+UP+时间）
   - 长按快捷操作（稍后再看/详情/UP 页）
   - 焦点边框

**验收**：
- [x] 主页可浏览推荐/热门/动态
- [x] 无限滚动正常
- [x] 菜单键刷新
- [x] 卡片长按快捷操作

**测试**：
- [x] `HomeViewModelTest`（单元，14 tests）：数据加载、分页、错误处理、超时、刷新清除 error
- [x] `RecommendVideoRepositoryTest`（单元）：Mock API，验证推荐/热门逻辑
- [x] `SmallVideoCardTest`（插桩）：显示内容、点击、长按快捷操作
- [x] `MainScreenTest`（插桩）：导航切换、退出确认

**实现备注**：
- `AndroidManifest` 添加 `LEANBACK_LAUNCHER`，否则 App 不出现在 TV 桌面
- `UserPanel` 用 `Dialog` 替代 `AnimatedVisibility`，避免 D-Pad 焦点穿透
- `SmallVideoCard` 长按逻辑：`Card(onClick, onLongClick)` 内置参数 + `BackHandler` 返回键关闭 + `.onFocusChanged` 失焦自动关闭 + `releaseLongPress` flag 防误触
- `HomeViewModel` 监听 `(uid, isLogin)` 变化，切换用户时清空并重新加载推荐/热门/动态
- `UserSwitchScreen` 重做为 `LazyColumn` 列表布局，`LifecycleResumeEffect` 从登录页返回时自动刷新用户列表
- 网络加载超时/错误统一处理：`withTimeout(10_000L)` + `xxxError` 状态字段 + `ListFooterTip` 组件（loading/error/no-more 三态）
- `CancellationException` re-throw 保护：`TimeoutCancellationException` 设 error，普通取消 re-throw 不吞掉
- BV 风格图标：`ic_play_count` / `ic_danmaku_count` / `ic_up` 替代 Material Icons
- 首页置顶 Tab 默认 `Dynamics`

**Bug 修复（P1-2 轮 5）**：
- Prefs `collect` 竞态修复：去掉 `init()` 中的 `dataStore.data.collect`，所有写入已通过 `PrefDelegate.setValue` 同步更新内存，`collect` 反向同步不仅多余且在部分写入时用旧快照覆盖内存（如 `saveToPrefs` 逐字段写入 9 个凭证时，`uid` 写完后 `collect` 触发，用「有新 uid 但旧 sessData」的快照覆盖内存）
- `upsertUser` 修复：`userDao.update(user)` 时 `user.id` 为 null（`addUser` 构造的 `UserEntity` 无 id），Room `@Update` 按 id 匹配导致静默失败。改为更新从 DB 查出的 existing entity（有 id）
- 经验进度条计算修复：`exp / nextExp` 改为 `(exp - currentMin) / (nextExp - currentMin)`，B 站 API 的 `next_exp` 是下一级门槛而非剩余（见 `docs/user/info.md`），`AccountUiState` 新增 `currentMin` 字段
- 切换账号等级不同步修复：`setCurrentUser()` 先重置 level/exp/currentMin/nextExp 为 0，再调 `refreshUserInfo()` 从网络拉取新用户数据

### P1-3 分区与影视 ✅

> **数据层已迁移**（P0-4）：`UgcRepository`、`PgcRepository`、`SeasonRepository` 已在 `:bili-api` 中实现并含集成测试。DI 已注入（`NetworkModule`），UI/ViewModel 已实现。

**已完成**：
1. ✅ `UgcContent`：15 分区顶部导航（中文名称）
   - `UgcViewModel`：分页加载、切换分区、刷新、超时/错误处理
   - `UgcGrid`：4 列网格 + 无限滚动
   - 菜单键刷新
2. ✅ `PgcContent`：6 分类顶部导航（番剧/国创/电影/纪录片/电视剧/综艺）
   - `PgcViewModel`：轮播图加载 + Feed 分页加载
   - `PgcCarousel`：自动旋转轮播图（D-Pad 左右切换，获焦点暂停）
   - `SeasonCard` / `SeasonCardData`：番剧卡片组件（封面 + 评分 + 标题）
   - 轮播图占位避免异步加载导致内容下移
3. ✅ `FocusSaver`：通用焦点恢复组件
   - 从详情页返回后恢复焦点到之前点击的卡片
   - 适用于所有 5 个网格页面（Recommend / Popular / Dynamics / UGC / PGC）
4. ✅ 导航修复
   - `selectedDrawerItem` 使用 `rememberSaveable` 保留选中页面
   - `LeftNaviItem` 实现 `Serializable`
   - 首次初始化请求焦点到 TopNav，返回时不抢焦点

**未完成项**（后续阶段）：
- `PgcIndexScreen`：索引筛选（多维度 FilterChip）
- `AnimeTimelineScreen`：番剧时间表
- `FollowingSeasonScreen`：追番列表
- PGC Feed 排行行（`PgcFeedRankRow`）暂未实现

**验收**：
- ✅ 分区可浏览，Tab 切换流畅
- ✅ 影视分类可浏览，轮播正常
- ✅ 从详情页返回焦点恢复到之前点击的卡片
- [ ] 索引筛选可用（后续阶段）
- [ ] 时间表可用（后续阶段）

**测试**（13 个单元测试）：
- `UgcViewModelTest`（6）：init 加载 / loadMore 追加 / switchType 切换 / error 保留数据 / refresh 刷新 / hasMore 阻止重复加载
- `PgcViewModelTest`（7）：init 加载含轮播图 / loadMore 追加 / switchType 切换 / error 保留数据 / refresh 刷新 / hasMore 阻止 / 轮播图加载失败不阻塞 Feed

### P1-4 视频详情页（UGC） ✅

> **数据层已迁移**（P0-4）：`VideoDetailRepository`、`LikeRepository`、`CoinRepository`、`FavoriteRepository`、`OneClickTripleActionRepository` 已在 `:bili-api` 中实现并含集成测试。DI 已注入，UI/ViewModel 已实现。

**已完成**：
1. ✅ `VideoDetailScreen`（916 行）：
   - 封面、标题、统计（播放/弹幕/点赞/投币/收藏/发布日期）
   - UP 信息（头像+名称，点击跳转 `UserSpaceRoute`）
   - 关注按钮（关注/取关 UP）
   - 点赞/投币/收藏按钮（长按一键三连）
   - 标签 Chips（点击导航到 `SearchResultRoute(keyword)`）
   - 简介展开（2 行 ↔ 全文）
   - 分 P 列表（始终显示 LazyRow `PartButton`，超过 5 个显示网格按钮 → `VideoPartListDialog`）
   - UGC 合集列表（LazyRow `PartButton`，超过 5 个显示网格按钮 → `VideoEpisodeListDialog`）
   - 相关视频行（LazyRow `SmallVideoCard`，与首页一致）
   - `ScreenFocusSaver` 焦点恢复（所有可聚焦元素接入）
   - Loading / Error 状态处理
2. ✅ `VideoDetailViewModel`（404 行）：
   - 详情数据加载（`withTimeout(15s)` + `CancellationException` re-throw）
   - 点赞/投币/收藏/一键三连/关注操作
   - 收藏夹列表加载 + 快速收藏到默认夹
   - `VideoDetailUiEffect`：`ShowToast` / `NavigateToSeason`
3. ✅ `VideoDetailRepository`（DI 已注入 `NetworkModule`）
4. ✅ `LikeRepository` / `CoinRepository` / `FavoriteRepository` / `OneClickTripleActionRepository`（均已迁移+注入）

**未完成项**（后续阶段补全）：
- ✅ 收藏夹选择弹窗 `FavoriteFolderDialog`（已收藏时点击收藏按钮弹出 FilterChip 选择收藏夹）
- ✅ 分 P >5 时网格弹窗 `VideoPartListDialog` + UGC 合集 `VideoEpisodeListDialog`（TabRow 分页 + 2 列网格 + TabRow 焦点恢复）
- ✅ `UserSpaceScreen` / `UpSpaceScreen`（用户信息 + 投稿视频网格；与原版 BV 一致，名称头像由路由传入，不调用 getUserInfo API）
- ✅ `LikeRepositoryTest`（单元，11 tests）/ `CoinRepositoryTest`（单元，11 tests）/ `OneClickTripleActionRepositoryTest`（单元，7 tests）— Mock BiliHttpApi 单例
- ⬜ `VideoDetailScreenTest` / `LikeButtonTest` 插桩测试

**验收**：
- ✅ 详情页完整显示
- ✅ 点赞/投币/收藏可用，长按三连
- ✅ 分 P /合集切换
- ✅ 收藏夹选择弹窗
- ✅ UP 主页可用

**测试**：
- ✅ `VideoDetailViewModelTest`（单元，7 tests）：加载成功/失败、点赞成功/失败、投币成功/失败、aid 参数匹配
- ✅ `VideoDetailRepositoryTest`（集成）：已在 `:bili-api` 迁移
- ✅ `FavoriteRepositoryTest`（集成）：已在 `:bili-api` 迁移

### P1-5 番剧详情页（PGC） ✅

> **数据层已迁移**（P0-4）：`SeasonRepository`、`VideoDetailRepository.getPgcVideoDetail()` 已在 `:bili-api` 中实现。DI 已注入。

**任务**：
1. `SeasonDetailScreen`（`PgcFeatureScreen.kt`）：
   - ✅ 封面、标题、风格标签、简介、播放按钮、追番按钮
   - ✅ 正片列表（横向 LazyRow，含"上次看到"标记）
   - ✅ 附加分集（PV/SP，遍历 `seasonDetail.sections`）
   - ✅ 季选择器（横向 Chip 行，点击切换 `onSwitchSeason`）
2. ✅ `SeasonDetailViewModel`（`app/viewmodel/pgc/SeasonDetailViewModel.kt`）
3. ✅ `SeasonRepository`（DI 已注入 `NetworkModule`）
4. 播放逻辑：`onPlay` 续播上次分集或播放第一集，`onPlayEpisode` 播放指定分集
5. 焦点恢复：`ScreenFocusSaver` 接入所有可聚焦元素（封面/播放/追番/分集/季切换）

**验收**：
- ✅ 番剧详情完整显示
- ✅ 季切换可用
- ✅ 追番可用
- ✅ 续播逻辑正确

**测试**：
- ✅ `SeasonDetailViewModelTest`（单元）：7 个测试（加载成功/失败、追番/取消追番、播放续播、季切换）

### P1-6 播放器（VOD 核心） ✅

> **数据层已迁移**（P0-4/P0-5）：`VideoPlayRepository`（播放地址/弹幕/字幕/蒙版/心跳/缩略图）已在 `:bili-api` 中实现；播放器引擎（`AbstractVideoPlayer`/`ExoMediaPlayer`/`BvVideoPlayer`）已在 `:player` 中迁移。DI 已注入，UI/ViewModel 已实现。

**已完成任务**：
1. ✅ `VideoPlayerScreen`：
   - `BvVideoPlayer`（Media3 画面）
   - `DanmakuPlayerCompose`（弹幕层）
   - 常显迷你进度条（可选）
   - `VideoPlayerController`（所有覆盖层）
2. ✅ `VideoPlayerController`：D-pad 键位映射
   - 播放/暂停、快进快退（加速）、上下集
   - 双击返回退出
   - 自定义快捷键处理
3. ✅ `ControllerVideoInfo`：顶部标题+时钟，底部进度条+按钮
   - `VideoProgressSeek`（Canvas 进度条）
   - `VideoShot`（缩略图预览）
4. ✅ `MenuController`：4 Tab 设置侧栏
   - `PlaySpeedMenu` / `PictureMenu` / `DanmakuMenu` / `ClosedCaptionMenu`
5. ✅ `VideoListController`：分集列表
6. ✅ `RelatedVideosController`：相关视频
7. ✅ ViewModel 拆分：
   - `PlayerViewModel`（播放控制、状态、心跳）
   - `PlayerMenuViewModel`（菜单设置）
   - `DanmakuViewModel`（弹幕加载、配置、蒙版）
   - `SubtitleViewModel`（字幕加载、配置）
   - `VideoListViewModel`（分集列表）
8. ✅ `VideoPlayRepository`（迁移）：播放地址、弹幕、字幕、蒙版、心跳、缩略图
9. ✅ 自定义快捷键系统（`PlayerCustomShortcuts` + Catalog，配置 UI 待 P1-9）
10. ✅ 播放结束动作（Pause/PlayNext/PlayRelated/Exit）
11. ✅ 断点续播、付费试看提示

**已完成测试**：
- ✅ `PlayerMenuViewModelTest`（单元）：菜单设置状态
- ✅ `DanmakuViewModelTest`（单元）：弹幕加载、配置更新、蒙版同步
- ✅ `SubtitleViewModelTest`（单元）：字幕加载、配置
- ✅ `VideoListViewModelTest`（单元）：分集列表、上下集查找
- ✅ `PlayerCustomShortcutsTest`（单元）：序列化、反序列化、normalize、别名兼容
- ✅ `VideoInfoRepositoryTest`（单元）：StateFlow 传播、更新/重置

**待补充测试**：
- ✅ `PlayerViewModelTest`（单元，35 tests）：init/togglePlayPause/seek/playNewVideo/updatePlaySpeed/listener callbacks/checkAndPlayNext/updateMediaProfile/detachPlayer/trySendHeartbeat
- ✅ `PlayDataTest`（单元，53 tests）：fromPlayUrlData 解析/plus 合并去重排序/DashVideo-DashAudio 字段映射
- ✅ `ApiSignTest` + `BuvidTest`（单元）：WBI 签名/buvid 生成
- ⬜ `VideoPlayerControllerTest`（插桩）：D-pad 键位、快进加速、自定义快捷键触发
- ⬜ `MenuControllerTest`（插桩）：4 Tab 切换、设置项交互
- ⬜ `DanmakuMenuTest`（插桩）：类型开关 All 联动、大小/透明度滑块
- ⬜ `VideoProgressSeekTest`（插桩）：进度条绘制、焦点
- ⬜ `VideoShotTest`（插桩）：缩略图显示

**已知问题**：
- ~~`PlayerViewModel` ~900 行，超出 300 行规范（核心播放 VM，后续可进一步拆分）~~ 不再拆分，保持现状

**Bug 修复（P1-6 轮次）**：
- 画质 ID 误用为编码 ID：`PlayData.fromPlayUrlData()` 中 `codecId = it.id` → `codecId = it.codecId`
- HEVC 编码匹配：`VideoCodec` 删除 `HVC1` 枚举值，`HEVC` 通过 `prefixes = ["hev1", "hvc1"]` 同时匹配两种 codec string
- playurl 端点风控：`injectCookies()` playurl 请求仍注入 `SESSDATA`+`DedeUserID`，只跳过 `buvid3`（与原版 BV 对齐）
- v_voucher 风控响应处理：检测到 `data` 含 `v_voucher` 时抛出 `RiskControlException`，避免反序列化失败
- Debug info 帧率删除：`ExoMediaPlayer` debugInfo 中移除 `@ ${frameRate}fps`
- 长按 Enter 修复：KeyDown 长按开菜单，KeyUp 才触发播放/暂停（避免长按时误触发）
- 切集时弹幕/字幕不重载：`PlayerViewModel` 暴露 `videoSwitchEvent: SharedFlow`，UI 层收集后协调重载
- lastPlayedCid 不匹配断点续播：仅在 `historyCid == currentCid` 时才应用断点续播
- 切视频时旧 playData 泄漏：`playNewVideo` 开头清空 `playData = null` 并重置全部 UI 状态
- isBuffering 错误时未清除：所有设置 `PlayerState.Error` 的地方同时 `isBuffering = false`
- SubtitleViewModel 内联 HttpClient：改为 Hilt 注入共享 `HttpClient` 单例

### P1-7 搜索 ✅

> **数据层已迁移**（P0-3/P0-4）：`SearchRepository` 已在 `:bili-api` 中实现（热词/建议/搜索+筛选）；`SearchHistoryDao` + `SearchHistoryRepository` 已在 `:data` 中实现。DI 已注入，UI/ViewModel 已实现。

**已完成**：
1. ✅ 搜索输入页（`SearchInputContent`，嵌入 MainScreen）：
   - 搜索输入框 + `SoftKeyboard`（6×6 网格）
   - 热词/建议切换（Tab 切换）
   - 搜索历史（增删，长按删除）
   - 3 列布局，与 Home/UGC/PGC 一致
2. ✅ 搜索结果页（`SearchResultContent`，独立 NavHost 路由）：
   - 4 类 Tab（视频/番剧/影视/用户）
   - 菜单键筛选弹窗（视频 Tab + Web API 时）：排序/时长区间
   - 无限滚动（`hasMore` 基于 API 返回 `numPages` 判断）
   - 去重逻辑（基于 aid/seasonId/mid 生成 key）
   - `ListFooterTip` 统一 loading/error/no-more 三态
3. ✅ `SearchInputViewModel`：热词/建议/历史
4. ✅ `SearchResultViewModel`：4 类型并行加载 + 分页 + 去重 + 筛选
5. ✅ `SearchRepository`（迁移 + `SearchTypeResult` 含 `hasMore` 字段）
6. ✅ `SearchHistoryDao` 持久化
7. ✅ `VideoDetailScreen` Tag 点击导航到 `SearchResultRoute(keyword=tag.name)`

**验收**：
- ✅ 搜索可用，热词/建议正常
- ✅ 搜索历史增删
- ✅ 结果四类切换
- ✅ 筛选可用

**测试**：
- ✅ `SearchInputViewModelTest`（单元，10 tests）：热词加载/失败、建议加载、历史增删、搜索触发
- ✅ `SearchResultViewModelTest`（单元，9 tests）：4 类型并行加载、分页追加、去重、hasMore 判断、筛选重搜、错误处理

### P1-8 个人页 ✅

> **数据层已迁移**（P0-4）：`ToViewRepository`、`HistoryRepository`、`FavoriteRepository`、`SeasonRepository`、`UserRepository`（关注列表）已在 `:bili-api` 中实现并含集成测试。DI 已注入，UI/ViewModel 已实现。

**已完成**：
1. ✅ `PersonalContent`（4-Tab 容器）：
   - TopNav（稍后再看/历史/收藏/追番，按首选项排序）
   - AnimatedContent 横向滑动切换
   - 菜单键刷新当前 Tab
2. ✅ `ToViewScreen`（稍后再看）：
   - 按 `progress == -1` 分为"未看完"/"已看完"两组
   - 4 列网格 + 分区标题
   - 卡片显示播放进度条
3. ✅ `HistoryScreen`（历史）：
   - 4 列网格 + 无限滚动（cursor 分页，距离底部 20 条触发）
   - 卡片显示播放进度条（`progress/duration`，`-1` 表示已看完显示满条）
4. ✅ `FavoriteScreen`（收藏）：
   - 顶部收藏夹 Tab 栏（LazyRow）+ 下方 4 列视频网格
   - 切换收藏夹自动加载该夹视频
   - 无限滚动（page 分页）
5. ✅ `FollowingSeasonScreen`（追番）：
   - 5 列网格 + 无限滚动（page 分页）
   - 菜单键筛选弹窗（类型：番剧/影视，状态：全部/想看/在看/看过）
   - `SeasonCard` 卡片
6. ✅ `PersonalViewModel`（单 VM 管理 4 Tab）：
   - 4 Tab 各自独立加载/分页/错误处理
   - `withTimeout` + `CancellationException` re-throw
   - 删除稍后再看、切换收藏夹、筛选追番等操作
7. ✅ `SmallVideoCard` 进度条支持：
   - `VideoCardData` 新增 `progress: Float?` 字段
   - `CardCover` 底部 3dp 进度条（白色半透明背景 + 主题色前景）
8. ✅ DI：`ToViewRepository` + `HistoryRepository` 注入 `NetworkModule`
9. ✅ MainScreen 集成：`PlaceholderContent("个人")` 替换为 `PersonalContent`

**未完成项**（后续阶段补全）：
- ✅ `FollowScreen`（关注 UP 列表）— `FollowViewModel` + `FollowedUserCard` 网格，UserPanel 入口
- ✅ `ToViewScreen` 删除按钮 — `SmallVideoCard` 新增 `onRemoveWatchLater` 回调，长按显示删除按钮

**验收**：
- ✅ 个人页 4 Tab 可用
- ✅ 稍后再看增删
- ✅ 历史显示进度条
- ✅ 收藏夹切换
- ✅ 追番列表

**测试**：
- ✅ `PersonalViewModelTest`（单元，16 tests）：ToView 加载/失败/删除/刷新、History 加载/分页/失败/刷新、Favorite 加载/切换/失败、FollowingSeason 加载/分页/失败/筛选

**视频卡片长按操作统一**：
- ✅ `SmallVideoCard` 支持四个长按操作：稍后再看(`onAddWatchLater`) / 详情(`onGoToDetailPage`) / UP页(`onGoToUpPage`) / 移除稍后再看(`onRemoveWatchLater`)
- ✅ 所有使用 `SmallVideoCard` 的页面（推荐/热门/动态/历史/收藏/稍后再看/搜索/相关视频/用户空间）均已接入全部可用回调
- ✅ `WatchLaterViewModel` 通用 ViewModel，通过 `hiltViewModel()` 在任意页面获取，提供 `addToView()` 能力

### P1-9 设置页 ✅

> **数据层已实现**（P0-3）：`Prefs` 全部设置项 + `PrefEnums` 全部枚举已在 `:data` 中实现。UI/ViewModel 已实现。`GithubApi` 已迁移。

**已完成**：
1. ✅ `SettingsScreen`：左右分栏布局（左导航 LazyColumn + 右内容区）
   - 6 分类：音视频/界面/其他/存储/设备信息/关于
   - D-Pad Left 从内容返回导航
   - 导航项获焦时自动切换内容区
2. ✅ `AudioVideoSetting`：画质/编码/音轨/倍速/播放结束动作/快捷键/软解/FFmpeg 音频
   - `OptionDialog` 通用枚举选择弹窗
3. ✅ `UISetting`：启动页/首页 Tab/个人页 Tab/主题模式/显示视频详情/常显进度条/界面缩放
   - `UIDensityDialog` D-Pad 上下调节 density
4. ✅ `OtherSetting`：接口选择/上传崩溃日志(Switch)/日志查看/CDN 测速入口
5. ✅ `StorageSetting`：图片缓存/其他缓存/崩溃日志 大小显示与清理
   - `ConfirmDeleteDialog` 确认弹窗
6. ✅ `InfoSetting`：设备厂商/型号/系统/屏幕/SOC/内存/存储信息
   - 编解码信息按钮跳转 `MediaCodecRoute`
7. ✅ `AboutSetting`：当前版本/最新版本/检查更新
   - `UpdateDialog`：检查更新→下载→安装全流程
8. ✅ `PlayerCustomShortcutsDialog`：多阶段快捷键配置弹窗
   - Main（列表 + 添加/清空）→ CaptureKey（捕获按键）→ PickAction（选动作）→ PickActionValue（选参数值）→ ConfirmClear
9. ✅ `GithubApi` 迁移：`app/network/GithubApi.kt`（object 单例，Ktor OkHttp 引擎）
   - `GithubRelease` 实体：`app/network/entity/GithubRelease.kt`
   - 通过 `ghfast.top` 代理下载 APK
10. ✅ 设置组件（`app/ui/component/settings/`）：
    - `SettingListItem` / `SettingSwitchListItem` / `SettingsMenuSelectItem` / `OptionDialog`
    - `EnumDisplayNames.kt`：所有设置枚举的 displayName 扩展
11. ✅ `FileProvider` 配置：`AndroidManifest.xml` + `res/xml/file_paths.xml`
12. ✅ Navigation 路由：`MediaCodecRoute` + `SpeedTestRoute` + `LogViewerRoute`
13. ✅ 日志查看器（`LogViewerScreen` + `LogViewerViewModel`）：
    - 左右分栏：日志文件列表 + QR 码（手机扫码访问 HttpServer）
    - `HttpServer` 生命周期管理（进入页面启动、离开停止）
    - 其他设置页入口"查看日志"

**验收**：
- ✅ 所有设置项可读可写
- ✅ 快捷键配置可用
- ✅ 在线更新可用
- ✅ 缓存可清理

**测试**：
- ✅ `EnumDisplayNamesTest`（单元，20 tests）：10 个枚举的 displayName 非空 + 已知值
- ✅ `SettingsMenuNavItemTest`（单元，4 tests）：枚举数量/顺序/displayName
- ✅ `GithubReleaseTest`（单元，5 tests）：序列化/反序列化/未知字段忽略/列表解析
- ✅ `UpdateStatusTest`（单元，2 tests）：枚举数量/状态值
- ⬜ `SettingsScreenTest`（插桩）：导航、各项交互 — 延后到集成测试
- ⬜ `PlayerCustomShortcutsDialogTest`（插桩）：配置流程 — 延后到集成测试

### P1-10 CDN 测速与编解码信息 ✅

**已完成**：
1. ✅ `SpeedTestScreen`：WebView 嵌入 B 站测速页面
   - 注入登录 Cookie（SESSDATA/bili_jct/uid 等）
   - CSS 缩放适配 TV 大屏
   - 加载遮罩
2. ✅ `MediaCodecScreen`：编解码器信息展示
   - 左右分栏（解码器列表 + 详情）
   - 显示硬/软解、最大实例数、颜色格式、码率范围、帧率范围、各分辨率支持帧率
3. ✅ `CodecUtil`：编解码器解析与分类
   - `CodecInfoData`：编解码器详细信息数据类
   - `CodecType`（Encoder/Decoder）、`CodecMode`（Hardware/Software）、`CodecMedia`（Audio/Video）
   - `SupportedFrameRate`：特定分辨率下支持的帧率范围
   - 硬/软件判断逻辑兼容 API 21+（Q+ 用 `isSoftwareOnly`，低版本用名称匹配）

**验收**：
- ✅ 测速可用
- ✅ 编解码信息正确显示

**测试**：
- ⬜ `CodecUtilTest`（单元）：编解码器分类、硬件/软件判断 — 需设备运行（依赖 Android MediaCodec API）

### Phase 1 验收标准

**进度**：P1-1 ✅ P1-2 ✅ P1-3 ✅ P1-4 ✅ P1-5 ✅ P1-6 ✅ P1-7 ✅ P1-8 ✅ P1-9 ✅ P1-10 ✅ | Phase 1 全部完成

- [x] 完整 VOD 观看流程：登录 → 浏览 → 详情 → 播放（弹幕/字幕/画质）→ 历史
- [x] 搜索功能完整
- [x] 个人页功能完整（关注列表 + 稍后再看删除 + 收藏夹选择）
- [x] 设置页功能完整
- [x] `./gradlew test` 通过（2024 单元测试 + 106 插桩测试，全部通过）
- [x] JaCoCo 覆盖率全部达标（`./gradlew test jacocoAggregatedReport`），各模块 per-module 报告：`./gradlew :<module>:createDebugJacocoReport`（Android）/ `:jacocoTestReport`（JVM）
  - bili-api 90.1%（目标 90%）、bili-subtitle 98.5%（目标 90%）、player 96.0%（目标 80%）、data 88.2%（目标 85%）、danmaku 88.9%（目标 80%）、app 81.1%（目标 80%）
- [x] 插桩测试关键路径覆盖（106 tests pass）：SmallVideoCard / ListFooterTip / PlayStateTips / MenuListItem / PlaySpeedMenuList / ControllerVideoInfoTop / MainScreen / MainActivity / BVApplication / ExoMediaPlayer / OkHttpUtil / DanmakuPlayerCompose / MaskModifiers / CrashHandler / SearchHistoryDao / UserDao

**后续待补全项**（推迟到 P2+）：
- PGC 索引筛选 `PgcIndexScreen`（多维度 FilterChip）
- 番剧时间表 `AnimeTimelineScreen`
- PGC Feed 排行行 `PgcFeedRankRow`
- `VideoDetailScreenTest` / `LikeButtonTest` 插桩测试
- 播放器插桩测试（VideoPlayerController / MenuController / DanmakuMenu / VideoProgressSeek / VideoShot）
- App gRPC 接口集成测试（需新 access_token）
- 设置页插桩测试（SettingsScreen / PlayerCustomShortcutsDialog）
- **弹幕分段加载（`DM.DmSegMobile` grpc + Web `seg.so`）**：已于 P3-4 完成实现（见 P3-4 说明）


---

## Phase 2：新增核心功能（M3）

**目标**：直播 + 触屏 + 评论 + 主题 + 日志体系 + 崩溃监控。

### P2-1 直播观看 [P0] ✅

**已完成**：
1. ✅ `LiveHomeScreen`：直播首页（推荐 + 分区 Tab + 关注列表）
2. ✅ `LiveCategoryScreen`：直播分区列表
3. ✅ `LivePlayerScreen`：直播播放器
   - 复用 `VideoPlayerScreen` 架构，`isLive=true`
   - 隐藏进度条/缩略图/分集/倍速/画质
   - WebSocket 弹幕（`LiveDataWebSocket` 重写为 OkHttp WebSocket）
   - 直播心跳（30 秒间隔，认证成功后启动）
4. ✅ `LivePlayerController` / `LiveControllerInfo` / `LiveMenuController`：直播间信息与控制
5. ✅ `BiliLiveHttpApi` 迁移 + 新增直播首页/分区接口
6. ✅ `LiveRepository` 新建

**Bug 修复（直播弹幕 + HLS 播放）**：
- ✅ HLS 直播流 `#EXT-X-START` 导致 `BEHIND_LIVE_WINDOW`：自定义 `HlsPlaylistParserFactory` 剥离标签 + `ExoMediaPlayer.onPlayerError()` 内部自动恢复
- ✅ `getLiveDanmuInfo` 返回 -352 风控：`encApiSign()` 中添加 `getDanmuInfo` 路径的 WBI 签名 + `type=0`/`web_location=444.8` 参数
- ✅ WebSocket 发送 auth 后 EOF：从 Ktor WebSocket 改为 OkHttp WebSocket，显式设置 `Referer`/`Origin` 头
- ✅ auth 响应未校验：解析 `OP_AUTH_REPLY` 校验 `code==0` 后才启动心跳
- ✅ 多包拼接丢失弹幕：`handlePacketBytes` 循环读取帧中所有协议包

**验收**：
- 直播首页可浏览
- 直播可播放（HLS/FLV）
- 直播弹幕实时显示
- 在线人数显示

**测试**：
- ✅ `LiveRepositoryUnitTest`（单元，3 个）：FLV/AVC 优先、HLS fallback、最终流地址 fallback
- ✅ `BiliLiveHttpApiTest`（集成，9 个）：使用房间号 `1718159119` 验证房间初始化、房间信息、直播状态、播放流、弹幕历史、弹幕 Token、分区列表与分页
- ✅ `LiveDataWebSocketTest`（集成，1 个）：使用房间号 `1718159119` 验证 WebSocket 连接生命周期；直播期间无弹幕时允许超时退出
- ⬜ `LivePlayerScreenTest`（插桩）：直播播放、弹幕、信息显示
- ⬜ `LiveHomeScreenTest`（插桩）：列表、导航

**测试验证记录**：
- ✅ `./gradlew :bili-api:test`：823 个单元测试通过
- ✅ `./gradlew :bili-api:integrationTest --tests 'dev.frost819.newbv.biliapi.http.BiliLiveHttpApiTest'`：直播集成测试通过（`LiveDataWebSocketTest` 已删除）
- 真实网络测试依赖 `local.properties` 中的 B 站测试配置；WebSocket 测试不要求观察窗口内必须产生弹幕消息

### P2-2 触屏适配 [P0] ✅

**已完成**：
1. ✅ `core/focus/TouchClickable.kt`：为 TV Material3 组件补充触屏点击/长按
   - 使用单个 `pointerInput(detectTapGestures)` 避免多 `pointerInput` 冲突
   - 触屏点击时通过 `FocusRequester.requestFocus()` 请求焦点，使 Tab 切换等 focus 驱动逻辑在触屏模式下正常工作
2. ✅ 全部交互组件接入 `touchClickable`：
   - 设置页（SettingListItem / SettingSwitchListItem / SettingsMenuSelectItem / UpdateDialog / PlayerCustomShortcutsDialog）
   - 搜索（SearchResultFilter / SoftKeyboard / UpCard）
   - 登录（QrLoginContent）
   - 视频卡片（SmallVideoCard / SeasonCard）
   - 播放器（ControllerVideoInfo / VideoPlayerController / VideoListController / 菜单 MenuListItem / StepLessMenuItem）
   - 其他（Carousel / TopNav / UserPanel / FollowScreen / UserSwitchScreen / UserSelectCard / PgcFeatureScreen / LogViewerScreen / MediaCodecScreen / SettingsScreen / FavoriteScreen / FollowingSeasonScreen）
3. ✅ 播放器手势系统：
   - `PlayerGestureHandler.kt`：播放器手势（快进/亮度/音量/缩放）
   - `GestureTip.kt`：手势提示 UI
4. ✅ 播放器快捷栏图标替换为原版 BV vector drawable + `tint = Color.White`
5. ✅ 播放器 bug 修复（P2 轮次）：
   - 弹幕 play/pause/seek/speed 与播放器状态同步（`VideoPlayerScreen` 中 `LaunchedEffect` 监听 `playerState`/`isBuffering`，`onGoTime`/`onPlaySpeedChange` 同时调用 danmaku 方法）
   - cid=0 请求错误：`PlayerScreens.kt` 先 `loadVideoDetail` 获取正确 cid，再用 `actualCid` 加载弹幕/字幕/playurl
   - `PlayerViewModel.loadVideoDetail()` 更新 `_uiState.cid` 从 `detail.cid`
   - 视频铺满溢出：`RESIZE_MODE_FILL` → `RESIZE_MODE_FIT` + `fillMaxSize`
   - 设置菜单三级展开：二级菜单 `onClick` 中增加 `onFocusStateChange(MenuFocusState.Items)`
   - 播放器↔详情页路由：播放器内打开详情改为叠加（`navigateToVideoDetailFromPlayer` 保留播放器）；详情页播放用 `popUpTo<VideoPlayerRoute>{inclusive}` 保证播放器单例；`showVideoInfo=false` 时相关视频直接播放（`resolveVideoClickTarget`）
   - bvid 全链路传播：`VideoCardData` / `Routes` / `VideoDetailRepository` / `VideoInfoRepository` / `PlayerViewModel` / `VideoDetailViewModel` 增加 `bvid` 字段
   - 弹幕 toggle：开启时用 `DanmakuEntityDanmakuType.entries`（全部类型）而非从 Prefs 读取
6. ✅ UP 主页不再调用 `getUserInfo` API（与原版 BV 一致）：
   - `UserSpaceRoute` 增加 `name`/`face` 可选参数
   - `UserSpaceViewModel` 移除 `loadUserInfo`/`toggleFollow`/`isFollowing`，简化为纯视频列表加载
   - `UserSpaceScreen` 移除 loading/error/关注按钮，直接从路由参数显示名称头像
   - 全部 11 处调用点传入 `name`（详情页/播放器/关注列表传 name+face）
   - `BiliHttpApi.getUserInfo` 端点修复为 `/x/space/wbi/acc/info`（WBI 签名，旧端点已废弃）

**验收**：
- [x] 触屏设备可完整操作
- [x] 播放器手势可用
- [x] D-pad 模式不受影响
- [x] UP 主页正常显示（不再"加载用户信息失败"）

**测试**：
- ⬜ `PlayerGestureHandlerTest`（插桩）：手势识别、动作触发
- ⬜ `SmallVideoCardTouchTest`（插桩）：触屏点击 vs D-pad 焦点
- `InteractionTrackerTest`（P0-2 已有单元测试）

### P2-3 评论浏览与播放器交互 [P1] ✅

**任务**：
1. ✅ `CommentRepository`：主评论、楼中楼、评论点赞（Web/App HTTP，统一模型）
2. ✅ `CommentViewModel`：热门/最新、主评论分页、楼中楼按需展开、评论点赞
3. ✅ `CommentsDialog`：详情页与播放器共用评论弹窗，不新增评论路由
   - 详情页使用宽弹窗
   - 播放器使用右侧窄弹窗
   - 播放器弹窗打开前正在播放则暂停，关闭后恢复；打开前已暂停则保持暂停
4. ✅ 楼中楼默认隐藏，仅显示回复数量；点击后在根评论下方加载并展开
5. ✅ 详情页增加评论入口
6. ✅ 播放器增加两个控制按钮：
   - 交互键：打开点赞/投币/收藏弹窗
   - 评论键：打开评论弹窗
   - 交互键长按点赞：一键三连
7. ✅ 扩展 `VideoInfoRepository`，同步详情页与播放器的点赞/投币/收藏状态
8. ✅ App gRPC `Reply.MainList` / `Reply.DetailList` 已接入（P2-9 完成）

**验收**：
- ✅ 详情页可打开评论弹窗
- ✅ 播放器可打开评论弹窗
- ✅ 评论点赞状态与数量可更新
- ✅ 楼中楼默认隐藏，点击回复数后展开
- ✅ 播放器评论弹窗打开/关闭时正确暂停与恢复
- ✅ 播放器交互弹窗支持点赞/投币/收藏
- ✅ 点赞长按支持一键三连
- ✅ App gRPC 评论列表与楼中楼接口（P2-9 完成，`Reply.MainList` / `Reply.DetailList`）

**测试**：
- ✅ `CommentTest`（单元）：Web/App JSON 转换与默认值
- ✅ `CommentViewModelTest`（单元）：列表、分页、楼中楼、点赞
- ⬜ `CommentsDialogTest`（插桩）：排序、展开、点赞、弹窗焦点
- ⬜ `VideoPlayerCommentsTest`（插桩）：暂停/恢复与播放器按钮

### P2-4 黑夜/白天主题 + 品牌配色 [P1] ✅

**任务**：
1. ✅ `ThemeMode` 设置项（跟随系统/黑夜/白天）— P0-2 已实现，设置 → 界面 → 主题可切换
2. ✅ `BVTheme` 根据 `ThemeMode` + 系统 `uiMode` 切换 colorScheme（TV Material3 + Common Material3 双层）
3. ✅ 所有屏幕适配 light colorScheme（颜色资源检查）
4. ✅ 播放器始终深色（控制层/弹幕/字幕/封面遮罩保持固定黑白，不随浅色主题变化）
5. ✅ **品牌配色重构**（区分 B 站粉色，新增）：
   - 主色：低饱和蓝紫 `#7773AD`（`Primary` / `PrimaryStrong` / `PrimaryLight`）
   - 辅助色：低饱和青绿 `#5B9B94`（`Secondary`），用于直播/追番/动态/分区
   - 深色背景改为中性灰：`#171717` → `#222222` → `#303030`（避免大色块蓝紫背景）
   - 浅色背景保留冷灰蓝层级：`#F5F7FB` → `#FFFFFF` → `#E8ECF5`
   - 语义色：`surface` / `surfaceVariant` / `onSurfaceVariant` / `error` / `border` 全量补齐
6. ✅ **页面配色层级统一**（新增）：
   - 共享组件：`TopNav` 支持 `accentColor`（首页 primary、PGC/直播 secondary）、焦点/选中分离；`SmallVideoCard` / `SeasonCard` / `LiveRoomCard` 标题、元数据、状态色分层
   - 详情页：播放/当前分P 用 `primary`，点赞/投币/收藏选中态用 `primaryContainer`，关注用 `secondary`
   - PGC：播放 `primary`，追番/当前系列 `secondaryContainer`
   - 直播：分区标签/更多按钮用 `secondaryContainer`，在线状态 `error`
   - 设置/个人/用户：选中项 `primaryContainer`，开关/单选选中态 `primary`
   - 登录：状态文字按加载/成功/错误着色，二维码保持白色背景 + 主题边框
7. ✅ `focusedBorder` 默认色改为主题 `border`（原固定白/黑，浅色模式下不可见）
8. ✅ 配色预览模板 `docs/color-palette-preview.html`（深浅模式切换、品牌色/状态色/组件示例）

**验收**：
- ✅ 主题三档可切换（设置 → 界面 → 主题）
- ✅ 浅色主题下所有 UI 可读（无白字白底/黑色弹窗突兀）
- ✅ 跟随系统模式正确响应系统切换
- ✅ 深色模式背景为中性灰，无大面积蓝紫压迫感
- ✅ 高亮按钮文字与背景对比度达标（`onPrimaryContainer` / `onSecondaryContainer` 配对）
- ✅ 播放器视觉保持独立固定深色
- ✅ 品牌视觉明显区别于 B 站粉色

**测试**：
- ✅ `ThemeModeTest`（单元）：模式解析、isDark、fromOrdinal
- ✅ `ColorPaletteTest`（单元）：品牌色值 + 深浅主题文字/品牌按钮 WCAG 对比度 ≥ 4.5
- ⬜ `BVThemeTest`（插桩）：colorScheme 切换（待模拟器运行）
- ⬜ 各 Screen 在 light/dark 下的快照测试（可选，待后续）

### P2-5 视频卡片已播进度条 [P1] ✅

**任务**：
1. ✅ `SmallVideoCard` 增加进度条叠加层（3dp 白色半透明背景 + 主题色前景）
2. ✅ 数据源（均来自各页面接口返回值，无需本地存储）：
   - 历史页：接口数据 `progress` / `duration`（秒）
   - 稍后再看：接口数据 `progress` / `duration`（秒）
   - UP 页：接口数据 `playback_position`（0-100 百分比，Web API `/x/space/wbi/arc/search` 返回）
3. ✅ 进度条样式：3dp，白色半透明背景 + 主题色前景
4. ✅ 进度文字：有进度时显示 "12:34 / 34:56"，已看完显示 "已看完 / 34:56"

**验收**：
- ✅ 历史/稍后再看/UP 页卡片显示进度条
- ✅ 无观看记录的卡片不显示
- ✅ 有进度的卡片同时显示进度文字

**测试**：
- ✅ `SpaceEntityTest`（单元）：`playback_position` 解析与转换
- ⬜ `SmallVideoCardProgressTest`（插桩）：有/无进度条显示

### P2-6 播放器点赞/投币/收藏快捷键 [P1] ✅（并入 P2-3）

**任务**：
1. ✅ 自定义快捷键新增动作：`LikeVideo` / `CoinVideo` / `FavoriteVideo`（P2-3 已实现 `VideoInteractionDialog`，含交互按钮 + 长按一键三连）
2. ✅ `ControllerVideoInfo` 底部按钮行新增交互/评论图标
3. ✅ 操作反馈 Toast
4. ✅ 按钮状态实时更新（通过 `VideoSharedState` 同步）

**验收**：
- ✅ 快捷键可触发点赞/投币/收藏（通过交互弹窗）
- ✅ 控制栏按钮可用
- ✅ 操作有 Toast 反馈

**测试**：
- ⬜ `PlayerCustomShortcutActionTest`（单元）：新动作序列化
- ⬜ `ControllerVideoInfoTest`（插桩）：按钮点击、状态更新

### P2-7 诊断日志记录 [P1] ✅

**任务**：
1. ✅ 统一使用 Core Logger 记录诊断日志：
   - 输出到 Android Logcat
   - 关键日志带 `[NAV]` / `[PLAYBACK]` / `[CARD]` / `[INPUT]` 前缀
   - 不记录设置项变更，不记录 Cookie/Token 等敏感信息
2. ✅ 关键链路日志：
   - `NavHost` 记录页面跳转
   - Activity `dispatchKeyEvent` 记录所有按键输入
   - 播放器记录播放、暂停、seek、切集和关键控制操作
   - 视频卡片记录点击和相关视频操作
3. ✅ `CrashHandler` 捕获崩溃前 Logcat，写入崩溃日志
4. ✅ Ktor 网页端支持崩溃日志/手动日志查看与下载

**验收**：
- [x] 关键操作有普通诊断日志记录
- [x] 崩溃报告包含崩溃前 Logcat
- [x] 网页端可查看和下载崩溃日志/手动日志
- [x] 日志中不包含 Cookie、Token 等敏感信息

**验证记录**：
- ✅ AVD 真机验证：Application 启动日志、D-Pad 多次按键日志（无遗漏）、卡片点击 + 导航日志、播放器全链路日志（详情→弹幕→字幕→缓冲→就绪→播放）、bili-api `[bili-api][INFO]` println 日志进入 Logcat、CrashHandler 初始化 + 崩溃日志文件采集（含所有 tag 日志）
- ✅ 覆盖率：`AndroidLogger` 100%、`Loggers` 100%、`BiliLogger` 100%

**测试**：
- ✅ `LoggerTest`（单元）：各日志级别 / 懒加载 / 异常安全（mockkStatic Log）
- ✅ `BiliLoggerTest`（单元）：前缀验证 / 异常堆栈 / 全方法覆盖
- ✅ `LogFormatTest`（单元）：设备信息和崩溃日志头格式
- ✅ `CrashHandlerTest`（插桩）：崩溃日志、手动日志、Logcat 写入
- ✅ `HttpServerTest`（单元）：崩溃/手动日志查看与下载

### P2-8 崩溃监控本地体系 [P1] ✅

**已完成**（P0-2 + P0-8 + P2-7 + P2-8 轮次协同实现）：
1. ✅ `CrashHandler` 完善：
    - `Thread.setDefaultUncaughtExceptionHandler`
    - 记录堆栈 + 设备信息 + 崩溃前 Logcat
    - 写入 `logs_crash_YYYY-MM-dd_HH:mm:ss.log`（本地文本日志，永久保留）
    - 崩溃后优雅退出（转发给原始 `UncaughtExceptionHandler`）
2. ✅ `CrashUploader`（新增）：
    - 崩溃数据转为结构化 JSON 上传到 Cloudflare Worker（R2 Storage）
    - 双重保障：崩溃时 best-effort 同步上传（5s 超时）+ App 启动时异步重试上传
    - 上传成功后删除本地 JSON 副本，`.log` 文本日志保留
    - 无 token 或后端不可达时静默跳过，不影响用户体验
    - 使用 `HttpURLConnection`（core 模块无额外 HTTP 依赖）
3. ✅ 崩溃上传开关（设置 → 其他 → 上传崩溃日志）：
    - `crashReportEnabled`（Boolean，默认关闭），Switch 开关组件
    - token 从 `local.properties` 的 `crashReport.token` 注入 `BuildConfig.CRASH_REPORT_TOKEN`
4. ✅ ProGuard 保留 `SourceFile,LineNumberTable` 供 R8 mapping 还原堆栈
5. ✅ Ktor 网页端支持崩溃日志查看与下载（P0-8 已实现）
6. ✅ AVD 实测验证：触发崩溃 → JSON 生成 → 重启后异步上传 → R2 收到数据 → JSON 本地删除

**验收**：
- [x] 崩溃自动记录（本地 `.log` + `.json`）
- [x] 崩溃日志含交互上下文（Logcat）
- [x] 崩溃日志可上传到 Cloudflare Worker + R2（需用户手动开启）
- [x] 上传失败下次启动自动重试
- [x] 崩溃后优雅退出

**测试**：
- ✅ `CrashHandlerTest`（插桩，10 tests）：异常捕获、日志写入、手动日志、清理逻辑、handler 安装卸载、设备信息写入、Logcat 输出
- ✅ `CrashUploaderTest`（插桩）：JSON 构建、文件保存、上传成功/失败、pending 日志上传
- ✅ `PrefsTest` / `PrefDelegateTest`（单元）：`crashReportEnabled` 默认值与读写

### P2-9 App 接口接入与 Web/App 分离 [P1] ✅

**完成状态**：App gRPC 接口 12 个 RPC 全部接入；App HTTP 接口（独立端点 + 共享端点）全部补齐；Web-only 清单明确。所有 Repository 方法通过 `preferApiType` 参数显式选择 Web/App 实现，无默认值，无自动降级。

**App gRPC 接口（12 个 RPC）**：

| RPC | Repository | 方法 |
|---|---|---|
| `Player.PlayViewUnite` | VideoPlayRepository | `getPlayData()` |
| `PlayURL.PlayView` | VideoPlayRepository | `getPgcPlayData()` |
| `DM.DmView` | VideoPlayRepository | `getSubtitle()`, `getDanmakuMask()` |
| `View.View` | VideoDetailRepository | `getVideoDetail()`, `getUgcPages()` |
| `Popular.Index` | RecommendVideoRepository | `getPopularVideos()` |
| `History.CursorV2` | HistoryRepository | `getHistories()` |
| `Search.Suggest3` | SearchRepository | `getSearchSuggest()` |
| `Search.SearchAll` | SearchRepository | `searchAll()` |
| `Search.SearchByType` | SearchRepository | `searchType()` |
| `Dynamic.DynVideo` | UserRepository | `getDynamicVideos()` |
| `Reply.MainList` | CommentRepository | `getComments()` |
| `Reply.DetailList` | CommentRepository | `getReplies()` |

**App HTTP 接口（独立端点）**：播放心跳、点赞、投币、一键三连、推荐、用户空间视频、视频截图、PGC 详情、追番/取消、追番列表、时间表、热搜词、稍后再看增删/列表、App TV QR 登录。

**App HTTP 接口（共享端点 + access_key）**：点赞/投币/收藏状态检查、收藏夹操作、关注/取关、关注列表、评论点赞。

**Web-only 接口**：用户信息、检查关注状态、PGC 轮播图/Feed/索引、分区推荐、直播全部接口、Web QR 登录、短信登录。

**未实现的 gRPC**：无。`DM.DmSegMobile`（弹幕分段）已于 P3-4 完成（Web `getDanmakuSeg` + App `getDanmakuSegment` + ViewModel 分段加载）。

**测试验证记录**：
- ✅ `./gradlew :bili-api:test`：823 个单元测试通过
- ✅ `./gradlew :app:testDebugUnitTest`：559 个单元测试通过
- ✅ `./gradlew :bili-api:jacocoTestReport`：bili-api 覆盖率 89.0% instruction / 90.9% line / 74.5% branch
- ✅ 新增集成测试全部通过（22 个）：BiliHttpApiTest App 端接口 + 评论 + 杂项（15）、BiliLiveHttpApiTest 直播列表/推荐/关注/分区/房间信息（5）、VideoPlayRepositoryTest 弹幕蒙版 Web/App（2）
- ⚠️ `getRegionFeedRcmd` 接口返回 -400（B 站可能已调整参数要求），集成测试暂移除；`addSeasonFollowApp` 直连返回 -404，由 `UserRepositoryTest` 的 Repository 层测试覆盖

**任务（已完成）**：
1. ✅ 以原版 BV 的 `bili-api` 实现为首要参考，逐个 Repository 补齐 App 分支
2. ✅ 对照 `docs/PRD.md` §8.2 和 `docs/bilibili-API-collect-master/docs/`，核对每个功能的 RPC/HTTP、请求字段、分页游标和响应转换
3. ✅ 完善 gRPC 公共基础设施：
   - `ChannelRepository` 的创建、关闭、账号切换和 token 更新
   - `authorization`、`x-bili-metadata-bin`、`x-bili-device-bin`、`x-bili-local-bin`、`x-bili-network-bin` metadata
   - gRPC 状态和 B 站业务错误统一转换为可识别异常
4. ✅ 按功能迁移 App 接口，核心链路全覆盖
5. ✅ 对照原版 BV 和 API 文档补齐其他 Repository 的 App 实现；无可靠 RPC 的功能建立 Web-only 清单
6. ✅ 保持 Web/App 接口显式分离：
   - `ApiType.Web` 只调用 Web HTTP
   - 有 App gRPC 或 App HTTP 的方法通过 `preferApiType` 选择实现；无 App 实现的方法不接收 `preferApiType`
   - 任一接口失败直接返回错误，不自动切换接口、不轮换 UA、不重试另一接口
7. ✅ 完善 App 接口集成测试凭证配置，核心 RPC 通过真实环境验证；无凭证时单元测试可独立运行

### Phase 2 验收标准

- [x] 直播可完整观看（首页/分区/播放/弹幕）（P2-1 ✅）
- [x] 触屏设备可完整操作（P2-2 ✅）
- [x] 评论可浏览（详情页 + 播放器内）（P2-3 ✅）
- [x] 主题三档可切换（P2-4 ✅）
- [x] 视频卡片进度条显示（P2-5 ✅）
- [x] 播放器快捷键点赞/投币/收藏（P2-3 ✅，通过 VideoInteractionDialog 实现）
- [x] 诊断日志记录与查看（P2-7 ✅）
- [x] 崩溃监控可用（P2-8 ✅）
- [x] App 接口接入完整，Web-only 清单明确（P2-9 ✅）
- [x] `./gradlew test` 通过（core 43, data 73, bili-subtitle 6, player 19, danmaku 34, bili-api 823, app 559 单元测试通过；bili-api 集成测试已分离为 `integrationTest` 任务），覆盖率全部达标

---

## Phase 3：增强与打磨（M4）

**目标**：P2 功能 + 稳定性 + 性能优化。

> **剩余任务细则**：[P3剩余任务计划.md](P3剩余任务计划.md)（推进顺序、弹幕双通道方案、检查表）

### P3-1 缓存满阈值自动清理 [P2] ✅

> 实现时需求简化：不区分图片/其他缓存，统一为单一缓存模型（用户确认）。

**已完成**：
1. ✅ `app/util/CacheManager`：无状态缓存管理器（构造参数可注入，纯 JVM 可测）
   - 统一管理 `cacheDir/image_cache`（Coil 图片缓存）+ `cacheDir/update_downloader`（更新包）
   - `cacheSize()` 跨目录统计、`clearAll()` 手动清空（保留目录）
   - `checkCache(preserve)`：阈值检查入口；`evictLru()` 跨目录按 lastModified 从旧到新删除（近似 LRU），清理至阈值 80% 防频繁触发
   - `preserve` 参数保护刚下载待安装的 APK 不被误删
2. ✅ 双层清理机制：
   - Coil 层：`BVApplication` 按 `Prefs.cacheThreshold` 构建 `DiskCache.maxSizeBytes`，每次图片写入原生 LRU 淘汰（自动清理关闭或阈值不限时设为 1TB 等同不限制）
   - CacheManager 层：文件级兜底，覆盖阈值调低/开关切换/更新包目录（无内建淘汰）
3. ✅ 触发时机：App 启动（`BVApplication.onCreate`）+ 缓存写入后（`UpdateDialog` 下载完成）+ 设置变更立即生效
4. ✅ 存储设置页（3 项 + 日志）：
   - 缓存阈值：50/100/200/500MB/不限制 弹窗选择，默认不限制（`cache_threshold`，0 = 不限制）
   - 手动清理缓存：显示总大小，确认弹窗后清空
   - 自动清空缓存：Switch 开关，默认开（`cache_auto_clean`，关闭后完全不自动清理）
   - 日志占用（原"崩溃日志"）：大小显示 + 确认弹窗清理（与缓存清理弹窗样式一致）

**测试**：
- ✅ `CacheManagerTest`（单元，13 个）：大小统计、LRU 最旧优先/达标即停、preserve 保留、阈值触发、开关关闭跳过、不限制跳过、设置变更即时生效、手动清理

**测试验证记录**：
- ✅ `./gradlew :app:testDebugUnitTest`：CacheManagerTest 13/13 通过
- ✅ `./gradlew ktlintCheck` / `detekt` / `assembleDebug` 通过

### P3-2 播放器同时观看人数 [P2] ✅

> 实现说明：直播部分（人气）在直播功能开发时已先行完成；点播数据源不用详情 `stat`（不含实时在线数），使用独立接口；显示位置由"与时钟并列"调整为标题正下方 meta 行。

**已完成**：
1. ✅ bili-api 在线人数接口（Web/App 双通道）：
   - Web `GET /x/player/online/total`（无需 WBI 签名/登录）：`OnlineTotal` 实体含 `total/count/show_switch`，按 UP 主展示开关选择文案
   - App `GET app.bilibili.com/x/v2/view/video/online`（host 触发拦截器自动 appkey+sign）：`OnlineTotalApp.online.total_text`
   - `VideoDetailRepository.getOnlineTotalText(preferApiType)` 分流，App 端剥离"人在看"后缀统一契约
   - app 层 `VideoInfoRepository.getOnlineWatchingText()`：失败静默返回 null，不影响播放
2. ✅ PlayerViewModel 响应式观察者：
   - 监听 `uiState.cid` 变化（`map/filter/distinctUntilChanged/collectLatest`）：直进、详情返回、播放器内切集均即时拉取，无固定等待
   - 60s 周期刷新（PRD 要求）；切集瞬间 collectLatest 取消旧 cid 在途请求
   - 请求失败保留旧文案静默降级；`detachPlayer` 停止观察者
3. ✅ UI：`ControllerVideoInfoTop` / `LiveControllerInfoTop` 标题正下方 meta 行（双人形图标 `ic_player_watching` + 小字），空值隐藏
4. ✅ 直播侧复用既有 WebSocket `ONLINE_RANK_COUNT` 实时更新人气，样式与点播统一

**测试**：
- ✅ `OnlineTotalTest`（单元，10 个）：JSON 解析、show_switch 三分支降级、App 实体解析与缺省
- ✅ `VideoDetailRepositoryUnitTest`（+6）：Web/App 分流、开关关闭返回 null、bvid 透传、错误传播、后缀剥离
- ✅ `PlayerViewModelTest`（+6）：冷启动即拉、60s 周期刷新、失败保旧文案、切集即时重拉（校验新 aid/cid）、cid 就绪零等待触发、无关状态变化去重
- ✅ 插桩：`ControllerVideoInfoTopTest`（+2 显示/隐藏）、`LiveControllerInfoTest`（新增 3 个）
- ✅ 集成：`BiliHttpApiTest` 新增 Web/App 在线人数真实请求用例（需凭证）

**测试验证记录**：
- ✅ `./gradlew :app:testDebugUnitTest :bili-api:test`：1419 个测试全部通过
- ✅ `./gradlew ktlintCheck` / `detekt` 通过

### P3-3 自定义快捷键触发提示 [P2] ✅

> 实现调整：原计划用系统 Toast，实际复用播放器 `PlayerTip` 组件（左下角堆栈）显示浮层，视觉与交互更统一。
>
> 后续调整：
> - `SetPlaybackSpeed` 改为 `TogglePlaybackSpeed`（在 1x 与目标倍速之间 toggle），排除 1x 倍速档位
> - 移除 `shortcutTriggerTips` 设置开关，快捷键提示始终显示
> - "上一个"/"下一个"文案改为"播放上一集"/"播放下一集"
> - 分集边界提示（"没有上一集"/"没有下一集"）复用同一 `shortcutTipText` 通道

**任务**：
1. ✅ 快捷键触发时在播放器左下角显示浮层提示动作名（`PlayerTip` + `shortcutTipText` 状态）
2. ✅ ~~设置开关~~（已移除，提示始终显示）
3. ✅ 提示时长 1.5 秒（`PlayerConstants.PLAYER_TIP_DURATION_MS`）
4. ✅ 分集边界提示（无上一集/下一集时显示"没有上一集"/"没有下一集"）

**实现要点**：
- `PlayerViewModel.showShortcutTip()`：触发时更新 `uiState.shortcutTipText`，1.5s 后自动清除
- `VideoPlayerController.executeCustomShortcut()`：执行动作后调 `onShowShortcutTip()` 回调
- `SkipTips`（`SkipTip.kt`）：接受 `shortcutTipText` 参数，渲染 `PlayerTip`（`Icons.Outlined.SettingsRemote`）
- 新提示覆盖旧提示（同一时刻仅显示最新一条）
- 动作集精简同步完成：删除 ShowInfo/OpenVideoList/TogglePlayPause 及除 SetPlaybackSpeed 外的全部参数化动作；弹幕防遮挡改为简单动作 ToggleDanmakuMask；旧持久化绑定解析时静默丢弃

**测试验证记录**：
- ✅ `PlayerCustomShortcutsTest` / `PlayerCustomShortcutCatalogTest` / `PlayerCustomShortcutsStoreTest`：随动作集精简重写，含遗留动作兼容降级用例
- ✅ `./gradlew :app:testDebugUnitTest` 通过

### P3-4 性能优化

> 细则见 [P3剩余任务计划.md](P3剩余任务计划.md) §3。

**任务**：
1. ✅ 弹幕分段加载（Web HTTP `/x/v2/dm/wbi/web/seg.so` + App gRPC `DmSegMobile`）— 见下方实现说明
2. 列表滚动 60fps 优化（Profiler 分析卡顿）
3. ✅ 启动速度优化（≤ 2 秒）— BiliHttpApi Ktor client 改 `by lazy`、gRPC Channel 改异步初始化，Application.onCreate 从 713ms 降到 125ms（2026-09-02 模拟器 TotalTime ~3600ms，真机/TV 待测）
4. ✅ 内存泄漏检测（LeakCanary 已引入，2026-09-02 模拟器实测通过：首页→详情→播放器→切集→返回→再进→直播进出→设置子页，无泄漏）

> **弹幕分段加载实现说明（2026-08 完成，参考 blbl / fantasytyx-bv 项目）**：
>
> - **引擎收编**：AkDanmaku 源码（125 文件，MIT）从 `com.github.Frost819:AkDanmaku:1.0.4` 收编进 `danmaku-engine/` 模块，包名保持 `com.kuaishou.akdanmaku`，依赖 gdx 1.12.0 + ashley 1.7.4；该模块豁免 detekt（第三方源码）
> - **clearData API**：akdanmaku 的 `updateData` 实为增量追加（`DataSystem.addItems`），原 `updateData(emptyList())` 无法清空数据（切集残留 bug）——新增 `DanmakuPlayer.clearData()` + `DataSystem.clearData()` 修复
> - **元数据驱动分段**：新增 Web `getDanmakuView`（`/x/v2/dm/web/view`，非 WBI）+ `VideoPlayRepository.getDanmakuMeta`，分段大小取服务端 `dmSge.pageSize`（不硬编码 6 分钟），`segTotal` 用于越界 clamp；App gRPC `DmViewReply` 无 `dmSge` 字段，故元数据固定走 Web 通道
> - **触发机制**：响应式 Flow（UI 喂 `seekerState.currentTime` → VM 内 map 段号 + distinctUntilChanged → collectLatest 加载），无 VM 内轮询循环，测试可用 `advanceUntilIdle`（规避踩坑 11.5.4）
> - **策略**：目标段 + 预取 1 段；单段 10s 超时 + 重试 2 次（1s/2s 指数退避）；失败不降级 XML（进度进入其他段时自然恢复）；切集 generation 防陈旧；弹幕关闭（dm/view state==1）时跳过加载
> - **内存**：不做滑动窗口裁剪（akdanmaku item 轻量、实体仅在播放窗口内创建），blbl 的 20 段窗口可作为后续增强

**验收**：
- 列表滚动无卡顿
- [x] 冷启动 ≤ 2 秒（模拟器 ~3600ms，真机/TV 待测）
- [x] 无内存泄漏（2026-09-02 LeakCanary 核心链路通过）

### P3-6 稳定性加固

> 细则见 [P3剩余任务计划.md](P3剩余任务计划.md) §4。

**任务**：
1. 焦点闪退治理（try-catch + 焦点重置）
2. 播放器异常统一处理（错误卡片 + 重试）
3. OOM 防护（图片/弹幕缓存限制）
4. 全局异常处理验证
5. 边界场景测试（网络断开/凭证过期/空数据）

**验收**：
- 连续使用 1 小时无崩溃
- 边界场景有合理处理

### P3-7 包体积优化

> 细则见 [P3剩余任务计划.md](P3剩余任务计划.md) §2。**先于 P3-4 执行。**

**任务**：
1. ✅ R8/ProGuard 配置优化（`isMinifyEnabled = true`）
2. ✅ 移除未使用依赖（lottie 已移除，ktor-client-websockets 确认无引用）
3. ✅ 资源压缩（`isShrinkResources = true`）
4. ✅ ABI 拆分评估（跳过：无大 native so，收益有限）
5. ✅ protobuf 代码裁剪（`ProtobufConfiguration.usedProtoFiles` 白名单）

**验收**：
- [x] APK 体积较优化前减小
- [x] 功能无回归

### Phase 3 验收标准

- [ ] 所有 P2 功能完成
- [ ] 列表滚动 60fps
- [ ] 连续使用 1 小时无崩溃
- [ ] 冷启动 ≤ 2 秒
- [ ] 包体积合理
- [x] `./gradlew test` 通过（core 43, data 73, bili-subtitle 6, player 19, danmaku 34, bili-api 823, app 559 单元测试通过；bili-api 集成测试已分离为 `integrationTest` 任务），覆盖率待达标
- [ ] 插桩测试覆盖关键路径

---

## 测试策略总结

### 覆盖率目标

| 模块 | 目标 | 重点 |
|---|---|---|
| `:bili-api` | ≥ 90% | 签名算法、数据解析、Repository |
| `:player` | ≥ 80% | 播放器状态机、URL 解析、编解码 |
| `:danmaku` | ≥ 80% | 弹幕解析、蒙版处理 |
| `:bili-subtitle` | ≥ 90% | BCC/SRT 解析编码 |
| `:data` | ≥ 85% | DAO、DataStore |
| `:app` ViewModel | ≥ 80% | 状态流转 |
| `:app` UI | ≥ 60% | 关键交互 |
| `:core` | ≥ 80% | 主题、日志、交互模式 |

### 测试工具

| 类型 | 工具 |
|---|---|
| 单元测试 | JUnit 5 + MockK + Turbine + Truth + kotlinx-coroutines-test |
| 插桩测试 | AndroidJUnit4 + Compose Test + MockK-Android |
| 覆盖率 | JaCoCo |
| 静态检查 | ktlint + detekt |
| 内存检测 | LeakCanary（debug） |

### CI 门禁

- PR 合并前：`./gradlew test ktlintCheck detekt` 必须通过
- 覆盖率低于目标：PR 标记警告
- 插桩测试：CI 用模拟器运行（可选），本地必须跑

---

## 风险与对策

| 风险 | 对策 |
|---|---|
| B 站接口风控 | Web/App 接口显式分离、正确签名与 metadata、buvid 优化；不做自动降级和 UA 轮换 |
| gRPC 接口变更 | proto 版本管理，及时跟进 |
| Compose TV 不稳定 | 关注 androidx.tv 版本，必要时降级 |
| Navigation 焦点问题 | 统一 FocusManager，充分测试 |
| 播放器 ViewModel 拆分困难 | SharedFlow 通信，逐个迁移验证 |
| 直播功能从零开发 | 复用原版底层 API，优先 UI 骨架 |
| 测试覆盖不足 | 每任务必带测试，PR 无测拒绝 |

---

## 附录：任务依赖关系

```
Phase 0:
  P0-1 配置 → P0-2/P0-3/P0-4/P0-5/P0-6 并行 → P0-7 → P0-8

Phase 1:
  P1-1 登录 → P1-2 主页 → P1-3 分区/影视 → P1-4 详情 → P1-6 播放器
                                ↓
                          P1-5 番剧详情 → P1-6 播放器
  P1-7 搜索（独立）
  P1-8 个人页（依赖 P1-1 登录）
  P1-9 设置（独立）
  P1-10 测速/编解码（独立）

Phase 2:
  P2-1 直播（依赖 P0-5 player + P0-6 danmaku）
  P2-2 触屏（依赖 P0-2 core）
  P2-3 评论（独立）
  P2-4 主题（依赖 P0-2 core）
  P2-5 进度条（依赖 P1-8 个人页）
  P2-6 快捷键（依赖 P1-6 播放器）
  P2-7 诊断日志（依赖 P0-2 core + P0-8 Ktor）
  P2-8 崩溃监控（依赖 P0-2 core + P2-7 诊断日志）
  P2-9 App gRPC 接入（依赖 P0-4 bili-api；参考 P1/P2 已有 Repository 实现）

Phase 3:
  全部基于 Phase 2 成果，独立推进
```
